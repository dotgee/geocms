pluginModule = angular.module "geocms.plugins", []

pluginModule.service "pluginService",
  [
    "$rootScope",
    ($root) ->
      plugins = {}

      plugins.addPlugins = (map) ->

      plugins
  ]